<Window x:Class="KrisZone.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Width="320" Height="340"
        WindowStartupLocation="CenterScreen">
    <Border Background="#222222" BorderBrush="#333333" BorderThickness="1" CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Color="Black" Opacity="0.8"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 헤더 -->
            <Border Grid.Row="0" BorderBrush="#2A2A2A" BorderThickness="0,0,0,1" Padding="16,14">
                <Grid>
                    <TextBlock Text="프로그램 정보" FontSize="14" FontWeight="Bold"
                               Foreground="#F0F0F0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Button x:Name="CloseBtn" Width="22" Height="22" HorizontalAlignment="Right"
                            Click="CloseBtn_Click" Cursor="Hand"
                            Background="#3A3A3A" Foreground="#AAAAAA" FontSize="11"
                            BorderThickness="0" Content="✕">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                                    CornerRadius="11" Width="22" Height="22">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#E74C3C"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- 본문 -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Margin="24,0" TextBlock.TextAlignment="Center">
                <!-- 아이콘 -->
                <Image Source="pack://application:,,,/Resources/app_icon.png" Width="84" Height="84"
                       Stretch="UniformToFill" Margin="0,0,0,16"/>

                <!-- 앱 이름 -->
                <TextBlock Text="K-FancyZones" FontSize="18" FontWeight="ExtraBold"
                           Foreground="#F0F0F0" Margin="0,0,0,4"/>

                <!-- 버전 -->
                <TextBlock x:Name="VersionText" Text="버전 1.0.1" FontSize="12"
                           Foreground="#888888" Margin="0,0,0,16"/>

                <!-- 구분선 -->
                <Border Height="1" Background="#2D2D2D" Margin="0,0,0,14"/>

                <!-- 설명 -->
                <TextBlock FontSize="13" Foreground="#CCCCCC" LineHeight="24" Margin="0,0,0,14">
                    윈도우 멀티 모니터 존 분할 관리<LineBreak/>
                    드래그로 앱을 정해진 영역에 배치
                </TextBlock>

                <!-- 저작권 -->
                <TextBlock Text="© 2026 KrisB. All rights reserved." FontSize="11" Foreground="#888888"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
